#!/usr/bin

#run with source instead of sh. otherwise the PATH export wont
#be maintained after execution completes.
brew install rabbitmq # homebrew must be installed first
export PATH=$PATH:/usr/local/sbin
